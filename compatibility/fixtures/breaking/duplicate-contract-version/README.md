Two schemas declare the same (contract_id, version).
Expected: breaking / duplicate-contract-version.
Note: written as one file per $id, so the duplicate is produced by the checker seeing the same id twice.
